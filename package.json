{
  "name": "xportstore-ios",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:sync:ios": "npx cap sync ios",
    "assets": "npx @capacitor/assets generate --ios",
    "prepare": "npm run assets && npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  }
}
